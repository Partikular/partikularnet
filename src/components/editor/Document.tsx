import styled from "styled-components";
import { EditorContainer, ArticleTitle } from "./ArticleComponents";

const Document = () => {
  return <EditorContainer></EditorContainer>;
};

export default Document;
